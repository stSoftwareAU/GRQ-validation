# GRQ Stock Prediction Validation Dashboard

The single source of truth for this project is the root [`../README.md`](../README.md).
Read it there — this file is only a pointer so the two copies cannot drift apart
(issue #758). The published dashboard entry point is [`index.html`](index.html).
